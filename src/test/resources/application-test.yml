endpoints:
  health:
    time-to-live: 0
spring:
  application:
    admin:
      enabled: false
  boot:
    admin:
      client:
        enabled: false
authentication:
    google:
        authUrlBase: /api/authentication/fake/google/auth
        tokenUrl: http://localhost:${server.port}/api/authentication/fake/google/token
        userProfileUrl: http://localhost:${server.port}/api/authentication/fake/google/user
        clientId: myGoogleClientId
        clientSecret: myGoogleClientSecret
