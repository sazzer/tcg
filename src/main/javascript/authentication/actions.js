/** Action key for initialising the list of authentication providers */
export const INITIALISE_AUTHENTICATION_PROVIDERS = 'AUTHENTICATION_PROVIDERS:INITIALISE';

/** Action key for resolving the list of authentication providers */
export const INITIALISE_AUTHENTICATION_PROVIDERS_RESOLVED = 'AUTHENTICATION_PROVIDERS:INITIALISE_RESOLVED';

/** Action key for recording the fact that we've just authenticated */
export const AUTHENTICATE = 'AUTHENTICATE';
