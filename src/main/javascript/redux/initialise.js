import { initialise as authentication } from '../authentication';
import { initialise as rules } from '../rules';

/** The initialisers to support */
export default [
    authentication,
    rules
];
